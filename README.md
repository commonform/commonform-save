```javascript
var save = require('commonform-save')
save(publisher, password, form, function (error, location) {
  // ...
})
```

```bash
npm i -g commonform-save
commonform-save "$publisher" "$password" ./example/some-form.json
```
